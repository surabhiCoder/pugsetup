import { CalculateMaxDirective } from './calculate-max.directive';

describe('CalculateMaxDirective', () => {
  it('should create an instance', () => {
    const directive = new CalculateMaxDirective();
    expect(directive).toBeTruthy();
  });
});
